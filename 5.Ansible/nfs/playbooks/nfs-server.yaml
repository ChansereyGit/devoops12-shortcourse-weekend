- name: NFS Server playbook 
  hosts: node1 
  become: true 
  tasks:
    - name: Running common tasks 
      import_tasks: ../tasks/common-task.yaml
    - name: Setup NFS Server
      when: false  
      import_tasks: ../tasks/nfs-server-task.yaml 