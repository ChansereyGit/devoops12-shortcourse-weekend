nfs_server: 
  ip: "{{ hostvars['node1']['private_ip'] }}"
 # ip: "{{ hostvars['node1']['ansible_default_ipv4']['address'] }}"
  mount_path: /srv/nfs_shared
nfs_client:
  mount_path: /mnt/nfs_shared
  ips: 
    - "{{ hostvars['node2']['ansible_default_ipv4']['address'] }}"
    - "{{ hostvars['node3']['ansible_default_ipv4']['address'] }}"