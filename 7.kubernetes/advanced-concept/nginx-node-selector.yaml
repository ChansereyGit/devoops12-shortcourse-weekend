apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-dpl
spec:
  replicas: 4 
  selector:
    matchLabels:
      app: nginx-app
  template:
    metadata:
      labels:
        app: nginx-app
    spec:
      nodeSelector: 
        kubernetes.io/hostname: worker01
      containers:
      - name: nginx-app
        image: nginx:latest
        ports:
        - containerPort: 80
